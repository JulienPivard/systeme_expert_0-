Pour lancer la configuration de cmake -G"Unix Makefiles" et du cmakecache avec les bonnes options :
lancer ./configure au niveau de la racine du projet,
pour exécuter le script de configuration.
( Mon script de configuration n'a été testé que sur des machines linux,
aucune idée du résultat si il est exécuté sur un autre système unix. )

Compilez avec make.

Lancer avec ./bin/Release/client lorraine.txt
